import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Welcome Admin," />
        <CardContent>Here's the complete data of users that booked the vacations with us</CardContent>
    </Card>
);